<nav class="nav">
  <div class="id-container">
    <form action="/today" method="post">
      <button type="submit" value="<%= data.location.name %>" name="submit" class="nav-btn" id="today">Today</button>
    </form>
    <form action="/hourly" method="post">
      <button type="submit" value="<%= data.location.name %>" name="submit" class="nav-btn" id="hourly">Hourly</button>
    </form>
    <form action="/forecast" method="post">
      <button type="submit" value="<%= data.location.name %>" name="submit" class="nav-btn <% curPage === 'forecast' ? 'active' : '' %>">3 Day Forecast</button>
    </form>
  </div>
</nav>